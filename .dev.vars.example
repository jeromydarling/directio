BETTER_AUTH_SECRET=generate-with-openssl-rand-hex-32
APP_URL=http://localhost:5173

# Sentry. Server-side DSN captures worker request + cron errors; the root loader
# also forwards it to the browser via window.ENV for client-side error/replay
# capture. Publishable value — set in production via `wrangler secret put SENTRY_DSN`.
# Unset → Sentry no-ops everywhere.
SENTRY_DSN=https://c1564f1ce71a011c9c72632440a13d59@o4511311951298560.ingest.us.sentry.io/4511507849216000
